name: Publish to NPM (Manual Dispatch)

on:
  # Manual trigger only - automatic releases handled by auto-release-from-pr.yml
  workflow_dispatch:

# Minimal permissions at workflow level (principle of least privilege)
permissions:
  contents: read

jobs:
  validate-environment:
    name: Validate Manual Deployment Environment
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      version_tag: ${{ steps.version.outputs.version_tag }}
      should-deploy: ${{ steps.check.outputs.should-deploy }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@1af3b93b6815bc44a9784bd300feb67ff0d1eeb3 # v6.0.0
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@2028fbc5c25fe9cf00d9f06a71cc4710d4507903 # v6.0.0
        with:
          node-version: '22'

      - name: Extract version from package.json
        id: version
        run: |
          PACKAGE_VERSION=$(node -p "require('./package.json').version")
          VERSION_TAG="v${PACKAGE_VERSION}"

          echo "version=${PACKAGE_VERSION}" >> $GITHUB_OUTPUT
          echo "version_tag=${VERSION_TAG}" >> $GITHUB_OUTPUT
          echo "✅ Extracted version from package.json: ${PACKAGE_VERSION} (tag: ${VERSION_TAG})"

      - name: Validate current branch is master
        id: check
        run: |
          CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD)

          if [ "$CURRENT_BRANCH" != "master" ]; then
            echo "❌ Manual deployment must be run from master branch"
            echo "Current branch: $CURRENT_BRANCH"
            echo ""
            echo "🚫 Deployment blocked for security reasons:"
            echo "   Manual deployments must be triggered from master branch"
            echo ""
            echo "🔧 To fix this:"
            echo "   1. Ensure you're on master branch before triggering"
            echo "   2. The deployment should be from latest master commit"
            echo ""
            echo "should-deploy=false" >> $GITHUB_OUTPUT
            exit 1
          else
            echo "✅ Manual deployment triggered from master branch"
            echo "should-deploy=true" >> $GITHUB_OUTPUT
          fi

  build-and-publish:
    name: Build, Validate and Publish to NPM
    runs-on: ubuntu-latest
    needs: validate-environment
    if: needs.validate-environment.outputs.should-deploy == 'true'

    # Minimal permissions for npm publishing and attestations
    permissions:
      id-token: write  # Required for npm provenance and SLSA attestations
      attestations: write  # Required for build provenance attestations
      contents: read  # Only read access needed for checkout

    outputs:
      version: ${{ needs.validate-environment.outputs.version }}
      version_tag: ${{ needs.validate-environment.outputs.version_tag }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@1af3b93b6815bc44a9784bd300feb67ff0d1eeb3 # v6.0.0

      - name: Setup Node.js
        uses: actions/setup-node@2028fbc5c25fe9cf00d9f06a71cc4710d4507903 # v6.0.0
        with:
          node-version: '22'
          # registry-url removed - not needed for Trusted Publishers with OIDC
          # Removing this prevents actions/setup-node from adding deprecated
          # 'always-auth=true' to .npmrc (causes warnings in npm 11+)

      - name: Install latest npm
        run: npm install -g npm@11.5.1

      - name: Validate version from package.json
        run: |
          PACKAGE_VERSION="${{ needs.validate-environment.outputs.version }}"
          VERSION_TAG="${{ needs.validate-environment.outputs.version_tag }}"

          echo "✓ Using version from package.json: $PACKAGE_VERSION"
          echo "✓ Version tag format: $VERSION_TAG"

          if ! [[ "$VERSION_TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Error: Invalid version format in package.json. Expected semantic version (1.2.3)"
            exit 1
          fi

          echo "✓ Version validation passed: $PACKAGE_VERSION"

      - name: Install dependencies
        run: npm ci

      - name: Run type check
        run: npm run typecheck

      - name: Run linter
        run: npm run lint

      - name: Run unit tests
        run: npm run test:unit

      - name: Build
        run: npm run build

      - name: Run integration tests
        run: npm run test:integration

      - name: Verify build artifacts
        run: |
          if [ ! -f "dist/index.js" ]; then
            echo "Error: Build artifact dist/index.js not found"
            exit 1
          fi
          echo "✓ Build artifacts verified"

      - name: Upload dist/ artifact for Docker build
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5.0.0
        # NOTE: SHA pinning required for security. IDE warnings are false positives.
        with:
          name: dist-${{ needs.validate-environment.outputs.version_tag }}
          path: dist/
          retention-days: 7
          if-no-files-found: error

      - name: Generate SBOM (Software Bill of Materials)
        run: |
          # Use npx to avoid npm install -g (Scorecard Pinned-Dependencies)
          npx --yes @cyclonedx/cyclonedx-npm@4.1.0 --output-file sbom.json
          echo "✓ SBOM generated: sbom.json"

      - name: Create tarball for attestation
        run: |
          # Note: npm is pinned via setup-node@v6.1.0 (Node.js 22)
          npm pack
          echo "TARBALL=$(ls *.tgz)" >> $GITHUB_ENV
          echo "✓ Package tarball created: $(ls *.tgz)"

      - name: Generate SLSA build provenance attestation
        uses: actions/attest-build-provenance@977bb373ede98d70efdf65b84cb5f73e068dcc2a # v3.0.0
        with:
          subject-path: ${{ env.TARBALL }}

      - name: Generate SLSA SBOM attestation
        uses: actions/attest-sbom@4651f806c01d8637787e274ac3bdf724ef169f34 # v3.0.0
        with:
          subject-path: ${{ env.TARBALL }}
          sbom-path: sbom.json

      - name: Verify npm >= 11.5.1 for Trusted Publishers
        run: |
          CURRENT_NPM=$(npm --version)
          echo "Current npm version: $CURRENT_NPM"
          if [ "$(printf '%s\n' "11.5.1" "$CURRENT_NPM" | sort -V | head -n1)" = "11.5.1" ]; then
            echo "✓ npm version $CURRENT_NPM is sufficient (>= 11.5.1)"
          else
            echo "❌ Error: npm version $CURRENT_NPM is too old"
            echo "npm >= 11.5.1 is required for Trusted Publishers"
            echo "Update Node.js version in workflow or manually install newer npm"
            exit 1
          fi

      - name: Publish to NPM with provenance (Trusted Publishers)
        run: npm publish --provenance --access public
        # Note: No NODE_AUTH_TOKEN needed - npm uses OIDC authentication automatically
        # Requires: id-token: write permission and npm >= 11.5.1

  create-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: [validate-environment, build-and-publish]
    if: needs.validate-environment.outputs.should-deploy == 'true'

    # Scorecard note: contents: write is REQUIRED for GitHub releases
    # This is isolated to a separate job with minimal scope:
    # - Runs ONLY after successful npm publish
    # - Has NO access to NPM_TOKEN or other secrets
    # - Uses pinned action (softprops/action-gh-release@SHA)
    # - Only creates release metadata (no code changes)
    # This is an acceptable use of write permissions per GitHub security best practices
    permissions:
      contents: write  # Required for GitHub Releases API
      actions: read  # Required for downloading artifacts

    steps:
      - name: Download dist/ artifact
        uses: actions/download-artifact@018cc2cf5baa6db3ef3c5f8a56943fffe632ef53 # v6.0.0
        # NOTE: SHA pinning required for security. IDE warnings are false positives.
        with:
          name: dist-${{ needs.validate-environment.outputs.version_tag }}
          path: dist/

      - name: Create dist tarball for release
        run: |
          tar czf dist.tar.gz dist/
          echo "✓ Created dist.tar.gz ($(du -h dist.tar.gz | cut -f1))"

      - name: Create GitHub Release (Draft)
        uses: softprops/action-gh-release@5be0e66d93ac7ed76da52eca8bb058f665c3a5fe # v2.4.2
        with:
          tag_name: ${{ needs.validate-environment.outputs.version_tag }}
          name: Release ${{ needs.validate-environment.outputs.version_tag }}
          files: dist.tar.gz
          body: |
            ## 📦 NPM Package Published (Manual Release)

            Version `${{ needs.validate-environment.outputs.version }}` has been published to npm:
            - Package: [@gander-tools/osm-tagging-schema-mcp](https://www.npmjs.com/package/@gander-tools/osm-tagging-schema-mcp)
            - Install: `npx @gander-tools/osm-tagging-schema-mcp@${{ needs.validate-environment.outputs.version }}`

            ## 🐳 Docker Image

            Docker images built from this release use the same `dist/` artifact as the NPM package:
            - Pull: `docker pull ghcr.io/gander-tools/osm-tagging-schema-mcp:${{ needs.validate-environment.outputs.version }}`
            - Build artifact: `dist.tar.gz` (attached to this release)
            - Provenance chain: Docker image → dist.tar.gz → NPM package (SLSA Level 3)

            ## 🔐 Provenance & Security

            This package was published with comprehensive supply chain security:

            ### NPM Provenance
            - [npm provenance](https://docs.npmjs.com/generating-provenance-statements) enabled
            - Verified build from GitHub Actions
            - Cryptographic attestations linking package to source

            ### SLSA Build Provenance
            - SLSA Level 3 build provenance attestations
            - Tamper-proof build metadata
            - Verifiable build environment and process

            ### SBOM (Software Bill of Materials)
            - CycloneDX SBOM generated and attested
            - Complete dependency transparency
            - Supply chain risk assessment

            ### Verification

            **Verify npm provenance:**
            ```bash
            npm audit signatures @gander-tools/osm-tagging-schema-mcp
            ```

            **Verify SLSA attestations:**
            ```bash
            gh attestation verify oci://ghcr.io/gander-tools/osm-tagging-schema-mcp:${{ needs.validate-environment.outputs.version }} --owner gander-tools
            ```

            ## 📋 Release Process

            This release was created manually using the "Publish to NPM (Manual Dispatch)" workflow.

            **Manual releases are used for:**
            - Emergency hotfixes
            - Testing deployment pipeline
            - Releases outside the normal release branch flow

            See [CHANGELOG.md](./CHANGELOG.md) for detailed changes.

            ---

            **Note:** This is a draft release. Review and publish when ready.
          draft: true
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
